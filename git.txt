Concept of got 
keep track of history
Takes snapshort
branches creation

Basic Command

$git init   // initialize Local Git Repository
$git add<file>		//Add files to index
$git ststus  		//check status of working tree 
$git commit 		// commit changes 
$git push 		// Push to remote Changes
$git pull 		// Pull Latest 
$git rm			// remove the file
$git branch <br_name>	// Create new branch 
$git checkout <br_naem>	// Change to new brench
$git merge <br_name>	// merge is the irectory with master directory

$git remote add origin <url of github repository > 	//Add the directory to github repository
$git push -u origin <directory name(master)>	  	// Upload the repository


$cd 			// Change the directory
$touch 			// Creating the file
$mkdir 			// Making new directory

Installion

Windows

http//git-scm.com/dawnload/win





